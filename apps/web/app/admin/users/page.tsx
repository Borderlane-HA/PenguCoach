"use client";import {FormEvent,useEffect,useState} from "react";import AppShell from "../../../components/AppShell";import {api} from "../../../lib/api";import {bi,useI18n} from "../../../lib/i18n";
export default function Users(){const{lang}=useI18n();const[rows,setRows]=useState<any[]>([]),[username,setUsername]=useState(""),[email,setEmail]=useState(""),[password,setPassword]=useState("");const refresh=async()=>{const data=await api<any[]>("/admin/users");setRows(data)};useEffect(()=>{void refresh()},[]);async function create(e:FormEvent){e.preventDefault();await api("/admin/users",{method:"POST",body:JSON.stringify({username,email,password,locale:lang==="de"?"de-DE":"en-US",role:"user"})});setUsername("");setEmail("");setPassword("");await refresh()}return <AppShell><h1>{bi(lang,"Benutzerverwaltung","User management")}</h1><section className="grid2"><form className="card stack" onSubmit={create}><h2>{bi(lang,"Benutzer anlegen","Create user")}</h2><label>{bi(lang,"Benutzername","Username")}<input required value={username} onChange={e=>setUsername(e.target.value)}/></label><label>E-Mail<input required type="email" value={email} onChange={e=>setEmail(e.target.value)}/></label><label>{bi(lang,"Temporäres Passwort","Temporary password")}<input required minLength={12} type="password" value={password} onChange={e=>setPassword(e.target.value)}/></label><button>{bi(lang,"Anlegen","Create")}</button></form><div className="card"><h2>{bi(lang,"Benutzer","Users")}</h2>{rows.map(x=><div className="between" style={{padding:"12px 0",borderBottom:"1px solid var(--border)"}} key={x.id}><div><strong>{x.username}</strong><small style={{display:"block"}}>{x.email} · {x.role}</small></div><div><span className="badge">Garmin {x.garmin}</span></div></div>)}</div></section></AppShell>}
